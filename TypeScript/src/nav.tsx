
// Each item in the nav will have an id and unique category that it represets
type navItem = {
    id: number
    category: string
    // color: number
    // Represent each with a color, stored in hexadecimal rbg values
    // v2.0 feature: show color code tab on screen and allow user to modify color codes
}